# Extension Guide

- Implement `ValueConverter` for custom type conversion.
- Implement `ValueValidator` for business validation.
- Implement `ExcelLifecycleListener` for lifecycle hooks.
- Implement `ExcelEngineAdapter` to add a new engine.
- Register adapter via Java SPI or Spring Bean.
