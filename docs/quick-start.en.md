# Quick Start

1. Add `autoexcel-spring-boot-starter`.
2. Annotate bean with `@ExcelSheet` and `@ExcelColumn`.
3. Inject `AutoExcel`.
4. Call `importFrom` / `exportTo` / `exportAsync`.

Supported engines:

- poi
- easyexcel
- jxls
